# 文档重分类与证据基线提交验证

日期：2026-09-09。用户明确授权先整理目录，再构建、测试和提交；本轮不推送。

## 范围

文档按治理、交付、契约、工具证据、架构、设计、工程协议、审计、验收和归档分类，入口见[文档导航](../README.md)。同步仓库说明、工程指引、当前有效 R3 计划的路径引用。历史执行结果保留原有路径用于追溯。

现有应用代码基线为 `42a65b43ef7721d567ff1cec5c3d74192e187771`。本次只调整文档及引用，不实现 R3 产品代码；现有测试通过不代表新生肖模型已经验收。

## 实际执行结果

| 检查                         | 结果                                                                      |
| ---------------------------- | ------------------------------------------------------------------------- |
| 生产构建 `npm run build`     | 通过，先于测试生成当前生产产物                                            |
| 类型检查 `npm run typecheck` | 通过，退出码0                                                             |
| 现有完整测试 `npm run test`  | 53文件、2133用例通过，退出码0；测试使用已有全局临时数据库隔离             |
| 文档格式                     | 使用项目已有Prettier；黄金YAML格式化前后解析值完全一致                    |
| R3计划静态门禁               | PASS，0 errors、0 warnings；更新分类后的路径与黄金文件字节哈希            |
| 链接与资源                   | 检查Markdown本地链接和HTML相对资源；迁移的4张架构截图与HEAD原文件字节一致 |
| 编码和差异                   | 严格UTF-8、无BOM、差异空白检查；无新增乱码                                |

构建保留现有的重复 `HexagramInfo` 自动导入、较大客户端分块及依赖弃用警告。测试输出包含预期失败分支日志、NuxtLink模拟环境和导航未实现提示；53文件均通过，不把这些提示描述为零警告。

项目故事中描述深层复制的正常术语会命中宽字符检测式，已核对其与HEAD原文一致；这是迁移前已有正常中文，不属于新增编码污染。工程指引中的检测命令本身也不作为污染证据。

## 尚未完成的事项

R3仍为Approved，待按新实现计划实施和验收。26个日期预期与60条干支分类是独立黄金数据，尚未接入R3新领域测试。全支持范围春节边界、独立历法引擎交叉核对和真实浏览器验收仍按[R3证据审阅](../audits/2026-09-09-r3-evidence-package-review.md)执行；本次项目基线测试不替代这些门槛。
